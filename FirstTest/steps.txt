1. Create instanace.tf including provider with access key and secret and resources

2. run "terraform init" to initialize terraform service

3. run "terraform plan" to see what will do before apply

4. run "terraform apply" to create the service

5. run "terraform destroy" to destroy the services